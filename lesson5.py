# Teplova // I need to output the first symbol of the word, the last symbol of the word, the second half of the string, write the word backward and the second symbol of the string
# Use "" if you write strings, use [] for outputting the task
example = "eloquence"
print(example[0])
print(example[-1])
print(example[4:])
print(example[::-1])
print(example[::2])